use async_trait::async_trait;
use crate::database::{connection::DatabasePool, seeder::Seeder};

pub struct {{name}};

#[async_trait]
impl Seeder for {{name}} {
    async fn run(&self, db: &DatabasePool) -> Result<(), sqlx::Error> {
        // Logika pengisian data di sini
        // Contoh:
        // sqlx::query("INSERT INTO users (name, email) VALUES (?, ?)")
        //     .bind("Admin")
        //     .bind("admin@example.com")
        //     .execute(&db.pool)
        //     .await?;
        
        println!("✅ Seeder {{name}} berhasil dijalankan.");
        Ok(())
    }
}
